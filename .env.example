# Travel Planner Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# AeroDataBox Flight API (via RapidAPI)
# Sign up at: https://rapidapi.com/aedbx-aedbx/api/aerodatabox
# Free tier: 600 API units/month, 2400 requests/month
# Supports flights up to 365 days ahead
# =============================================================================
AERODATABOX_API_KEY=your_rapidapi_key_here
AERODATABOX_HOST=aerodatabox.p.rapidapi.com

# =============================================================================
# Geoapify API (Geocoding, City Search, Timezone)
# Sign up at: https://myprojects.geoapify.com/
# 1. Create a free account
# 2. Create a new project
# 3. Copy the API key from the project dashboard
# Free tier: 3,000 requests/day
# =============================================================================
GEOAPIFY_API_KEY=your_geoapify_api_key_here

# =============================================================================
# Foursquare Places API (Food & Attractions)
# Sign up at: https://foursquare.com/developers/
# 1. Create a free developer account
# 2. Create a new app
# 3. Copy the API key (starts with "fsq3")
# Free tier: $200/month in credits (~20,000 regular API calls)
# =============================================================================
FOURSQUARE_API_KEY=fsq3xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Google Places API (Rich Place Details - Hours, Pricing, Tags)
# Sign up at: https://console.cloud.google.com/
# 1. Create a new project (or use existing)
# 2. Enable "Places API (New)" in APIs & Services > Library
# 3. Create credentials > API Key
# 4. (Recommended) Restrict the key to Places API only
# Free tier: $200/month credit (~5,000 Place Details requests)
# Pricing: https://developers.google.com/maps/documentation/places/web-service/usage-and-billing
# Note: Used to fetch business hours, pricing, and amenities. Falls back to Foursquare if not set.
# =============================================================================
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# =============================================================================
# TimezoneDB API (Optional - fallback for timezone lookup)
# Sign up at: https://timezonedb.com/register
# 1. Register for a free account
# 2. Copy the API key from your account page
# Free tier: 1 request/second, unlimited requests
# Note: Only used as fallback when Geoapify doesn't return timezone data
# =============================================================================
TIMEZONEDB_API_KEY=your_timezonedb_api_key_here

# =============================================================================
# Rate Limiting Configuration
# =============================================================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_WEATHER=100
RATE_LIMIT_MAX_FLIGHTS=50
RATE_LIMIT_MAX_ROUTING=200
RATE_LIMIT_MAX_GEOCODING=100
RATE_LIMIT_MAX_CITIES=100
RATE_LIMIT_MAX_PLACES=100

# =============================================================================
# Server Configuration (used by adapter-node)
# =============================================================================
PORT=3000
HOST=0.0.0.0
ORIGIN=http://localhost:3000

# =============================================================================
# Database Configuration
# Use absolute path for Docker deployment to match Dockerfile's /app/data
# =============================================================================
DATABASE_PATH=/app/data/cache.db
